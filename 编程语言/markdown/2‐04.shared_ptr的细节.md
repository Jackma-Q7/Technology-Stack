**shared_ptr的细节**

std::shared_ptr的尺寸大于裸指针,一般是裸指针的两倍，因为它内部既包含一个指向到资源的裸指针,也包含一个指向该资源的引用计数的裸指针

引用计数的内存是动态分配的

引用计数的递增和递减是原子操作

<img width="1196" height="505" alt="image" src="https://github.com/user-attachments/assets/6f18ba4b-95fe-4a57-84e6-fa8e2ed21c69" />

```cpp
#include <memory>
#include <iostream>

int main()
{
	int *p = new int(18);
	std::shared_ptr<int> sp(p);

	std::cout << "p " << sizeof(p) << " sp " << sizeof(sp);
}
```

```cpp
#include <memory>
#include <iostream>

class CTest
{
public:
	CTest(int i)
	{

	}

	CTest(int i, int j)
	{

	}
};

int Prcoess2()
{
	return 0;
}

void Process(std::shared_ptr<CTest> sp, int Ret)
{

}

int main()
{
	std::shared_ptr<int> sp = std::make_shared<int>(18);

	std::shared_ptr<CTest> sp1 = std::make_shared<CTest>(18);
	std::shared_ptr<CTest> sp2 = std::make_shared<CTest>(18, 18);

	//隐藏了三步操作
	/*1.new CTest(18);
	2.执行Process2()
	3.构造了shared_ptr对象*/

	Process(std::shared_ptr<CTest>(new CTest(18)), Prcoess2());

	/*1.构造make_shared
    2.Prcoess2()*/
	Process(std::make_shared<CTest>(18), Prcoess2());
}
```

 shared_ptr 管的是“这个指针指向的那块堆内存（对象）  