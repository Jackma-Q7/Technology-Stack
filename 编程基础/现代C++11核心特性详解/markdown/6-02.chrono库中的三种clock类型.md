## 1.system_clock
&emsp;&emsp;system_clock为系统时间(壁钟时间)，它的原点时间是UNIX 1970-01-01 00:00::00 UTC，它包括3个静态方法: now, to_time_t, from_time_t(与C 风格的时间相互转换) ，它的用法比较简单，当需要获取时间时，就可以使用system_clock

&emsp;&emsp;1.now() —— 获取当前时间

&emsp;&emsp;2.to_time_t() —— 转成C风格时间  

&emsp;&emsp;精度下降（纳秒 → 秒）  

&emsp;&emsp;3.from_time_t() —— 再转回chrono  

&emsp;&emsp;如果改了系统时间，system_clock的值也会变化，所以它是不稳定的`system_clock::is_steady`值为false

```cpp
#include <chrono>
#include <iostream>

int main()
{
    auto tp = std::chrono::system_clock::now();
    auto d = tp.time_since_epoch();

    std::cout << d.count() << std::endl;
    std::cout << time(NULL) << std::endl;

    time_t now = std::chrono::system_clock::to_time_t(tp);
    std::cout<<ctime(&now)<<std::endl;

    tm *info = localtime(&now);
    //2021-8-15 12:00:00

    char tmp[128] = { 0 };
    sprintf(tmp, "%d-%d-%d %d:%d:%d", 
        info->tm_year + 1900, 
        info->tm_mon + 1, 
        info->tm_mday, 
        info->tm_hour, 
        info->tm_min, 
        info->tm_sec);

    std::cout << tmp << std::endl;
}
```

1777251119411269930 
1777251119 
Mon Apr 27 00:51:59 2026 
2026-4-27 0:51:59

✅<font style="color:rgba(0, 0, 0, 0.88);"> 写法1（你现在用的）</font>

```cpp
time(NULL);
```

👉<font style="color:rgba(0, 0, 0, 0.88);"> 含义：</font>

+ <font style="color:rgba(0, 0, 0, 0.88);"> 不关心返回值写入哪里 </font>
+ <font style="color:rgba(0, 0, 0, 0.88);"> 只要返回值 </font>

✅<font style="color:rgba(0, 0, 0, 0.88);"> 写法2（等价写法）</font>

```cpp
time_t now;
time(&now);
```

👉<font style="color:rgba(0, 0, 0, 0.88);"> 含义：</font>

+ <font style="color:rgba(0, 0, 0, 0.88);"> 返回值给你 </font>
+  同时也写入 `now`

## 2.steady_clock 
&emsp;&emsp;steady_clock是处理器的tick时间(非壁钟时间)，原点是机器启动的时刻，steady_clock::now()返回的值绝不会小于上一次调用的值，即使改了系统时间，也会保证steady_clock::now()产生正确的值

&emsp;&emsp;它最适合**度量间隔(精度)，它只有一个静态now方法**。它经常被用来计算时间间隔，通过使用steady_clock命名空间的duration和time_point，来表示高精度的时间间隔

&emsp;&emsp;更改了系统时间并不会影响 steady_clock，所以它是稳定的，`steady_clock::is_steady` 值为true

```cpp
#include <chrono>
#include <iostream>
#include <thread>
int main()
{
   auto before = std::chrono::steady_clock::now();
   std::this_thread::sleep_for(std::chrono::seconds(1));
   auto after = std::chrono::steady_clock::now();

   std::cout << after.time_since_epoch().count() << std::endl;

   std::chrono::steady_clock::duration d = after - before;
   std::cout << d.count() << std::endl;

   std::chrono::duration<double, std::milli> d1 = after - before;
   std::cout << d1.count() << std::endl;
   
   
   //std::cout << std::chrono::system_clock::now().time_since_epoch().count() << std::endl;
}
```

<font style="color:rgba(0, 0, 0, 0.88);">4387431295141022 </font>

<font style="color:rgba(0, 0, 0, 0.88);">1000060472 </font>

<font style="color:rgba(0, 0, 0, 0.88);">1000.06</font>

## 3.high_resolution_clock
&emsp;&emsp;high_resolution_clock，它代表了当前系统的所支持的最高精度，它可能就是system_clock或steady_clock，是一个类型别名(不同平台实现不同)，它也只有一个now方法。使用场景跟steady_clock一致

```cpp
#include <chrono>
#include <iostream>
#include <thread>
int main()
{
    auto before = std::chrono::high_resolution_clock::now();
    std::this_thread::sleep_for(std::chrono::seconds(1));
    auto after = std::chrono::high_resolution_clock::now();

    std::chrono::duration<double, std::milli> d = after - before;
    std::cout << d.count() << std::endl;

}
```