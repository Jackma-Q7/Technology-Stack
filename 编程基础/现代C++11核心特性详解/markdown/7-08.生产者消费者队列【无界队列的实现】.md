```cpp
#include <thread>
#include <condition_variable>
#include <mutex>
#include <deque>
#include <iostream>
#include <future>

template<typename T>
class UnBoundedQueue
{
public:
    UnBoundedQueue()
    {
    }

    // 生产者：向队列中放入数据
    void Put(const T& x)
    {
        // 加锁：保护队列，防止多个线程同时修改队列
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            m_deqDatas.push_back(x);
        }

        // 通知消费者：队列中已经有数据了
        // notify_one：唤醒一个消费者线程
        // notify_all：唤醒所有等待的消费者线程
        m_notEmpty.notify_all();
    }

    // 消费者：从队列中取出数据
    T Get()
    {
        // unique_lock 可以配合 condition_variable 使用
        std::unique_lock<std::mutex> lock(m_mutex);

        /*
            如果队列为空，就阻塞等待。
            wait 会自动释放 lock，让生产者有机会加锁并放入数据。

            当生产者调用 notify_all / notify_one 后，
            消费者线程会被唤醒，并重新获得 lock。

            lambda 返回 true 时，说明队列不为空，可以继续执行。
        */
        m_notEmpty.wait(lock, [this] {
            return !m_deqDatas.empty();
        });

        // 取出队头元素
        T front = m_deqDatas.front();

        // 删除队头元素
        m_deqDatas.pop_front();

        return front;
    }

    // 判断队列是否为空
    bool Empty()
    {
        // 加锁：防止其他线程正在修改队列
        std::lock_guard<std::mutex> lock(m_mutex);
        return m_deqDatas.empty();
    }

    // 返回当前队列中的元素数量
    size_t Size()
    {
        // 加锁：防止其他线程正在修改队列
        std::lock_guard<std::mutex> lock(m_mutex);
        return m_deqDatas.size();
    }

private:
    std::deque<T> m_deqDatas;              // 底层容器：双端队列
    std::mutex m_mutex;                    // 互斥锁：保护队列
    std::condition_variable m_notEmpty;    // 条件变量：队列不为空时通知消费者
};

// 消费者线程函数
void c(UnBoundedQueue<int>* d)
{
    auto threadid = std::this_thread::get_id();

    while (true)
    {
        int i = d->Get();

        std::cout << "thread id " << threadid
                  << ", get value " << i << std::endl;
    }
}

int main()
{
    UnBoundedQueue<int> d;

    // 创建两个消费者线程
    std::future<void> ret =
        std::async(std::launch::async, c, &d);

    std::future<void> ret1 =
        std::async(std::launch::async, c, &d);

    // 生产者：放入10个数据
    for (int i = 0; i < 10; ++i)
    {
        d.Put(i);
        std::cout << "put data " << i << std::endl;
    }

    // 等待消费者线程结束
    // 注意：消费者线程是 while(true)，所以这里会一直等待
    ret.wait();

    return 0;
}
```

一个“典型输出”（但不是唯一）

```plain
put data 0
put data 1
put data 2
put data 3
put data 4
put data 5
put data 6
put data 7
put data 8
put data 9
thread id 111, get value 0
thread id 222, get value 1
thread id 111, get value 2
thread id 222, get value 3
thread id 111, get value 4
thread id 222, get value 5
thread id 111, get value 6
thread id 222, get value 7
thread id 111, get value 8
thread id 222, get value 9
```

&emsp;&emsp;该程序中生产者不会阻塞，消费者在队列为空时阻塞等待；数据按照FIFO顺序被消费，但由于多线程并发执行，线程ID和输出顺序是不确定的。

## 问题1：!m_deqDatas.empty()这部分在哪里进行修改的
👉 `m_deqDatas.empty()`**的结果确实会随着 **`push_back()`** 和 **`pop_front()`** 改变**

但注意：❗ `empty()` 本身不存储值，它只是“实时判断当前容器状态”