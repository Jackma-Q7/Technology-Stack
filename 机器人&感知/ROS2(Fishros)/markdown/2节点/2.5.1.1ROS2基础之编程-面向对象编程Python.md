## 2.5.1ROS2基础之编程:面向对象编程Python
### 概念
面向对象编程都可以创建类

类是对事物的封装

```
人类
属性：名字，身高，年龄
方法：吃饭睡觉，做运动
```
```
修士
属性：修为，体质
方法：修炼，施法，炼丹，飞升
```

### 实例化
```python
class PersonNode:
    def __init__(self, name:str, age:int )-> None:
        self.age = age
        self.name =name
    def eat(self, food_name: str):
        print(f'我叫{self.name}，今年{self.age}岁，我现在正在吃{food_name}')

def main():
#这步就叫实例化，说人话就是使用提供的参数以PersonNode为模板生成node
    node = PersonNode('法外狂徒张三',18)
# 这步叫调用对象的方法(函数)
    node.eat('鱼香肉丝')
```

### 继承
通过继承创建一个新的类真的!真的!真的很省事!

```python
from demo_python_pkg.person_node import PersonNode

class WriterNode(PersonNode):
    def __init__(self,book:str)-> None:
        print('WriterNode init方法被调用了')
        super().__init__(name,age)#调用父类
        self.book = book
        
def main():
    node = WriterNode('mjj',1,'论快速入狱')
    node.eat('鱼香肉丝')
```

例子

```python
import rclpy
from rclpy.node import Node

class PersonNode(Node):
    def __init__ (self,node name:str,name:str,age:int)->None:
        super().init(node name)
        self.age =age
        self.name = name
    def eat(self，food name:str):
        self.get_logger().info(f'我叫{self.name},今年{self.age}岁，我现在正在吃{food_name}')

def main():
    relpy.init()
    node =PersonNode('person node'，'法外狂徒张三'，18)
    node.eat('鱼香肉丝')
    rclpy.spin(node)
    rclpy.shutdown()
```