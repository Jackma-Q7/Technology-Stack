## 7.4.1使用话题初始化机器人位姿
**查看amcl话题的信息**

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765332004809-2bcb4a9a-c83e-44c3-b560-497a9565372b.png)

**使用命令来发布初始化位姿**

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765332053999-a68172c9-bb0c-4f32-adf8-b9cbf54a2f9a.png)

fishbot_application功能包初始化init_robot_pose.py

```python
from geometry_msgs.msg import PoseStamped
from nav2_simple_commander.robot_navigator import BasicNavigator
import rclpy


def main():
    rclpy.init()
    navigator = BasicNavigator()
    initial_pose = PoseStamped()
    initial_pose.header.frame_id = 'map'
    initial_pose.header.stamp = navigator.get_clock().now().to_msg()
    initial_pose.pose.position.x = 0.0
    initial_pose.pose.position.y = 0.0
    initial_pose.pose.orientation.w = 1.0
    navigator.setInitialPose(initial_pose)
    navigator.waitUntilNav2Active()
    rclpy.spin(navigator)
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

**setup.py**

**完成代码后，在setup.py中对该节点进行注册，然后在功能包清单文件中声明对nav2simple commander、rclpy和geometry msgs的依赖，最后重新构建功能包。**

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765332558358-9d6e7de5-6bfc-4b04-98f5-52c4c0c93976.png)

这里是ros2 run 