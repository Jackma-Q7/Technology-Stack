## 9.0.4.使用ROS2键盘控制FishBot并查看ODOM
### 一、启动FishBot
第一步还是启动FishBot的电源，接着打开终端，运行McroRos服务

```bash
sudo docker run -it --rm -v /dev:/dev -v /dev/shm:/dev/shm --privileged --net=host microros/micro-ros-agent:$ROS_DISTRO udp4 --port 8888 -v6
```

如因为网络问题无法启动可以尝试国内指令：

```bash
sudo docker run -it --rm -v /dev:/dev -v /dev/shm:/dev/shm --privileged --net=host registry.cn-hangzhou.aliyuncs.com/fishros/micro-ros-agent:humble udp4 --port 8888 -v6
```

等待FishBot自动接入，如果长时未接入，可以手动点击RST重启，如还未接入，请回到上一节配置助手处重新配置。

接入标志为，看到如下信息

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765763559083-af5a3e55-ae5e-406d-b1c1-b29156de6e89.png)

### 二、 测试键盘控制
**打开终端，输入**

```plain
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765763599857-84dc69ab-34dd-4a41-9738-5add4675ea02.png)

接着按x按键调节下线速度，降低到0.1左右，防止一下子太快飞出去。

接着尝试点击j按钮，机器人将逆时针转动,点击k或者空格，机器人将停在原地。

根据键盘提示，你可以尝试前进后退，左转右转等命令。

### 三、里程计信息
**我们的小车是带编码器的，可以实时输出里程计数据，使用指令**

```bash
ros2 topic echo /odom --field pose.pose
```

**就可以看到实时的机器人位置信息。****  
**** **<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765763647598-569774c6-1a01-4c8f-b6c8-918de8932c99.png)

### 四、使用rviz2可视化里程计数据
打开终端输入rviz2，即可打开rviz2界面，接着完成下面四步。

1. 修改fixedframe为odom
2. 点击ADD
3. 切换到By Topic，选择odom
4. 点击OK按钮

接着我们需要修改下odom的订阅模式，展开odom话题，将Reliable修改为Best Effort。修改成功后右边将出现里程计箭头。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765763704229-2dc931cd-3da0-468f-a66f-fabb53415df3.png)

此时控制小车移动，你将在rviz2中实时看到小车的轨迹，这个就是里程计。

**sudo apt install ros-humble-rviz-imu-plugin**

**查看imu话题，看三个轴**