## 6.3.2为机器人部件添加质量与惯性
真实的机器人部件肯定是有质量的，既然有质量，那么在运动的时候就会有惯性。质量可以直接使用多少千克表示，而旋转惯性则需要使用一个3x3的矩阵表示。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765159781223-af279ae8-8d1d-4100-8de6-a9bfed408b1b.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765159792950-5232592b-76e5-41be-9f1f-b150877f4226.png)

common_ inertia.xacro  

```xml
<?xml version="1.0"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro">

  <!-- ==========================
       盒体惯性矩（Box Inertia）
       参数：
         m 质量
         w 宽度
         h 高度
         d 深度
       ========================== -->
  <xacro:macro name="box_inertia" params="m w h d">
    <inertial>
      <mass value="${m}" />
      <inertia
        ixx="${(m/12) * (h*h + d*d)}"
        ixy="0.0"
        ixz="0.0"
        iyy="${(m/12) * (w*w + d*d)}"
        iyz="0.0"
        izz="${(m/12) * (w*w + h*h)}" />
    </inertial>
  </xacro:macro>


  <!-- ==========================
       圆柱体惯性矩（Cylinder Inertia）
       参数：
         m 质量
         r 半径
         h 高度
       ========================== -->
  <xacro:macro name="cylinder_inertia" params="m r h">
    <inertial>
      <mass value="${m}" />
      <inertia
        ixx="${(m/12) * (3*r*r + h*h)}"
        ixy="0"
        ixz="0"
        iyy="${(m/12) * (3*r*r + h*h)}"
        iyz="0"
        izz="${(m/2)  * (r*r)}" />
    </inertial>
  </xacro:macro>


  <!-- ==========================
       球体惯性矩（Sphere Inertia）
       参数：
         m 质量
         r 半径
       ========================== -->
  <xacro:macro name="sphere_inertia" params="m r">
    <inertial>
      <mass value="${m}" />
      <inertia
        ixx="${(2/5) * m * (r*r)}"
        ixy="0.0"
        ixz="0.0"
        iyy="${(2/5) * m * (r*r)}"
        iyz="0.0"
        izz="${(2/5) * m * (r*r)}" />
    </inertial>
  </xacro:macro>

</robot>

```

base.urdf.xacro添加惯性宏

```xml
<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

  <!-- 引入通用惯性宏 -->
  <xacro:include filename="$(find fishbot_description)/urdf/fishbot/common_inertia.xacro"/>

  <!-- 定义 base_xacro 宏 -->
  <xacro:macro name="base_xacro" params="length radius">

    <link name="base_link">
      ...
      <!-- 调用圆柱体惯性宏 -->
      <xacro:cylinder_inertia m="1.0" r="${radius}" h="${length}"/>
    </link>

  </xacro:macro>

</robot>

```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765161311961-d392156f-61b5-4133-b0cf-79551ab1fa9e.png)<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765161322230-f7f4c78b-a9b2-42fe-89bd-5a1f635b37b5.png)