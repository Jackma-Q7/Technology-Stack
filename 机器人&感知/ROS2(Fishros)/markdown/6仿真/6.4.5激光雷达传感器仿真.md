## 6.4.5 激光雷达传感器仿真
```xml
<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

  <!-- 激光传感器 Gazebo 插件宏 -->
  <xacro:macro name="gazebo_sensor_plugin">

    <gazebo reference="laser_link">

      <!-- 定义一个 Ray（激光雷达）类型传感器 -->
      <sensor name="laserscan" type="ray">

        <!-- 加载 Gazebo ROS 激光插件 -->
        <plugin name="laserscan" filename="libgazebo_ros_ray_sensor.so">

          <ros>
            <namespace/>
            <!-- 输出主题 remap 为 /scan -->
            <remapping>~/out:=scan</remapping>
          </ros>

          <!-- 输出类型为 ROS2 LaserScan 消息 -->
          <output_type>sensor_msgs/LaserScan</output_type>
          <frame_name>laser_link</frame_name>

        </plugin>

        <!-- 启动时就开启激光 -->
        <always_on>true</always_on>
        <visualize>true</visualize>
        <!-- 激光数据发布频率（5Hz） -->
        <update_rate>5</update_rate>

        <!-- 激光雷达在 link 中的位置 -->
        <pose>0 0 0 0 0 0</pose>

        <!-- 激光配置 -->
        <ray>

          <!-- 扫描角度配置 -->
          <scan>
            <horizontal>
              <samples>360</samples>
              <resolution>1.0</resolution>
              <min_angle>0.00000</min_angle>
              <max_angle>6.280000</max_angle> <!-- 360° -->
            </horizontal>
          </scan>

          <!-- 扫描距离范围 -->
          <range>
            <min>0.12</min>
            <max>8.0</max>
            <resolution>0.015</resolution>
          </range>

          <!-- 噪声配置 -->
          <noise>
            <type>gaussian</type>
            <mean>0.0</mean>
            <stddev>0.01</stddev>
          </noise>

        </ray>

      </sensor>

    </gazebo>

  </xacro:macro>

</robot>

```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765176275092-1d8b0115-5d4b-412c-ae33-9aedd9ba4283.png)

在fishbot.urdf.xacro中

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/57223890/1765176333020-7f8e4241-ffc3-40f3-a0a0-38ab20cf5f3d.png)
